import type { Metadata } from "next";
import "./globals.css";
import { comfortaa } from "./fonts/fonts";
import { AppUtils } from "./context/utils";
import { AppRemoveBg } from "./context/remove-bg";

export const metadata: Metadata = {
  title: "Loomibg",
  description: "Generated by Loomibg",
  other: {
    "google-adsense-account": "ca-pub-3582978571424183",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <head>
        {/* O Next vai injetar automaticamente a meta aqui */}
      </head>
      <body
        cz-shortcut-listen="true"
        className={`${comfortaa.className}`}
      >
        <AppRemoveBg>
          <AppUtils>

            {children}
            {/* <Toaster /> */}
          </AppUtils>
        </AppRemoveBg>
      </body>
    </html >
  );
}
